---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: step-ca-bootstrap-persistent
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-5"

---
# Note: This bootstrap job doesn't need RBAC permissions since it only
# writes to the PVC and doesn't interact with the Kubernetes API.
# The ServiceAccount is kept for consistency and potential future use.
