apiVersion: v1
kind: Service
metadata:
  name: traefik-casdoor-auth
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: casdoor-auth
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
