apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: casdoor-auth
spec:
  forwardAuth:
    address: http://traefik-casdoor-auth.traefik.svc.cluster.local:8080/verify
    authResponseHeaders:
      - X-Forwarded-User
