apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

components:
- ../../components/bootstrap-pvc
- ../../components/bootstrap-step-issuer

# NOTE: Make sure to bump this tag whenever you bump the chart version.
# Centralized image tag - applies to both StatefulSet (from Helm) and bootstrap Job
images:
  - name: cr.smallstep.com/smallstep/step-ca
    newTag: "0.29.0"

patches:
- path: patch-deployment.yaml
