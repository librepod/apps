apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

# Bootstrap the CA issuer using Step Certificates
# This component creates a ClusterIssuer that uses the Step CA as the certificate authority
components:
- ../../components/bootstrap-ca-issuer
