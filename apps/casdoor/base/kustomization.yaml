apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: casdoor

resources:
- namespace.yaml
- ocirepository.yaml
- helmrelease.yaml
- pvc.yaml

configMapGenerator:
- name: init-data
  files:
    - ./init_data.json  
  options:
    disableNameSuffixHash: true
