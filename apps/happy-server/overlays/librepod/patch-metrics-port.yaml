# Add metrics port to service for Prometheus scraping
apiVersion: v1
kind: Service
metadata:
  name: happy-server
spec:
  ports:
    - port: 9090
      targetPort: metrics
      protocol: TCP
      name: metrics
