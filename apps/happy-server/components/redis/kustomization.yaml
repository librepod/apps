# Redis component for Happy Server
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: happy-server

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/component: redis

resources:
- deployment.yaml
- service.yaml
- pvc.yaml
