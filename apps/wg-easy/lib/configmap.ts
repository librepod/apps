import * as kplus from 'cdk8s-plus-26';
import { Construct } from 'constructs'

// import * as dotenv from 'dotenv'
//
// const processEnv: { [key: string]: any } = {}
// dotenv.config({ processEnv })
//
// const result1 = dotenv.parse('BASIC=basic')
// console.log(result1)
//
// const result2 = dotenv.parse(Buffer.from('BASIC=basic'))
// console.log(result2)

// export default function(scope: Construct) {
//   const config = new kplus.ConfigMap(scope, 'cm-wg-easy');
//   config.addData('WG_HOST', 'ee.relay.librepod.org:6000')
//   config.addData('PASSWORD', 'admin')
// }
