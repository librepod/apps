apiVersion: v1
kind: Service
metadata:
  name: gogs-postgres
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gogs
    app.kubernetes.io/component: postgres
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
