apiVersion: batch/v1
kind: Job
metadata:
  name: job-step-ca-bootstrap-step-issuer
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: "5"
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 1
  parallelism: 1
  completions: 1
  template:
    metadata:
      name: job-step-ca-bootstrap-step-issuer
    spec:
      restartPolicy: Never
      serviceAccountName: step-ca-bootstrap-step-issuer
      containers:
      - image: cr.smallstep.com/smallstep/step-ca:0.29.0
        imagePullPolicy: IfNotPresent
        name: job-step-ca-bootstrap-step-issuer
        command: ["/bin/bash", "/scripts/step-ca-boostrap-step-issuer.sh"]
        envFrom:
          - configMapRef:
              name: cm-step-ca-bootstrap-step-issuer
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
          runAsNonRoot: true
          runAsUser: 1000
        volumeMounts:
          - mountPath: /scripts
            name: entrypoint
            readOnly: true
          - mountPath: /home/step
            name: ca-data
      dnsPolicy: ClusterFirst
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      volumes:
        - name: entrypoint
          configMap:
            name: cm-step-ca-bootstrap-step-issuer-script
            items:
              - key: step-ca-boostrap-step-issuer.sh
                path: step-ca-boostrap-step-issuer.sh
        - name: ca-data
          persistentVolumeClaim:
            claimName: step-certificates-data
