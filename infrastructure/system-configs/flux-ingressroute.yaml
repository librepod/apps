apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: flux-operator
  namespace: flux-system
spec:
  entryPoints:
  - web
  - websecure
  routes:
  - kind: Rule
    match: Host(`flux.${BASE_DOMAIN}`)
    priority: 1
    services:
    - name: flux-operator
      port: 9080
