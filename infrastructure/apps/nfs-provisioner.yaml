apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: nfs-provisioner
  namespace: flux-system
spec:
  interval: 10m
  url: oci://ghcr.io/librepod/marketplace/apps/nfs-provisioner
  ref:
    tag: "1.0.0"
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nfs-provisioner
  namespace: flux-system
spec:
  interval: 1h
  retryInterval: 2m
  timeout: 5m
  sourceRef:
    kind: OCIRepository
    name: nfs-provisioner
  path: ./overlays/librepod
  prune: true
  wait: true
